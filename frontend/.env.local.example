# NEXT_PUBLIC_API_BASE is an environment variable that stores the base URL for the API.
# URL points to the local development API running on port 8000.
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1